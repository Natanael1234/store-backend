import { IdConfigs } from '../id/id.configs';

const { MIN_ID, MAX_ID } = IdConfigs;

export class IdListConfigs {
  public static readonly MIN_ID = MIN_ID;
  public static readonly MAX_ID = MAX_ID;

  public static readonly MAX_LENGTH = MAX_ID;
}
